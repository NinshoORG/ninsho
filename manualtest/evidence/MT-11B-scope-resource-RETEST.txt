==============================================================================
NINSHO MANUAL SECURITY RETEST EVIDENCE: MT-11B
Real Resource-Level Scope Attack
Timestamp: 2026-09-07T17:06:00.000Z
Target: express-api (http://localhost:3000) & playground (http://localhost:4000)
Status: NOT APPLICABLE (N/A)
==============================================================================

1. ROUTE AUDIT & INVESTIGATION:
An exhaustive review of all route registrations across both runnable local applications was performed:
  - examples/express-api/src/app.ts:
    * GET /me                      -> auth.verify()
    * GET /admin/reports           -> auth.verify(), auth.requireRole('admin')
    * GET /users/:id/orders        -> auth.verify(), auth.requireOwner((req) => req.params?.['id'])
    * All /auth/* endpoints        -> Authentication / Session lifecycle
  - examples/playground/src/server.ts:
    * POST /api/authz/scopes       -> Internal interactive simulation harness that exercises
                                     Ninsho's requireScope() guard in-memory and renders a tabular audit.
                                     (Previously tested in baseline test MT-11).

2. FINDINGS:
Neither application declares a standalone live HTTP resource endpoint (e.g., GET /orders or POST /orders)
protected directly by Ninsho's auth.requireScope() middleware.

3. ADHERENCE TO TESTING CONSTRAINTS:
Per the user specification:
"If no suitable real resource endpoint exists, do NOT invent one and do NOT modify production code.
Mark MT-11B as NOT APPLICABLE and explain why."

In accordance with this directive:
  - No mock routes were injected into express-api or playground.
  - No production or example code was modified.
  - Scope checking functionality remains verified at the library level via the unit suite
    (packages/server/src/__tests__/middleware.test.ts) and at the playground demonstration level
    (POST /api/authz/scopes in MT-11).

4. CONCLUSION:
MT-11B is classified as NOT APPLICABLE (N/A) due to the absence of a dedicated scope-protected
HTTP resource route in the shipping example applications.
==============================================================================
