{
  "name": "@ninsho/playground",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive protocol explorer for Ninsho \u2014 shows the real bytes, and the real attacks failing.",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ninsho/client": "0.1.0",
    "@ninsho/server": "0.1.0",
    "@ninsho/webauthn": "0.1.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
