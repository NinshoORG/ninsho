{
  "name": "@ninshorg/playground",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive protocol explorer for Ninsho — shows the real bytes, and the real attacks failing.",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ninshorg/client": "0.1.0",
    "@ninshorg/server": "0.1.0",
    "@ninshorg/webauthn": "0.1.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
