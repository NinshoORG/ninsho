node_modules/
dist/
coverage/
*.tsbuildinfo

# Secrets — never commit real key material or environment files
.env
.env.*
!.env.example
*.pem
*.key

.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Local launch-video builds (brag / Hyperframes). Carries its own node_modules
# and an 80 MB ffmpeg binary — the rendered videos are distributed separately,
# never through this repository.
brag-output/
